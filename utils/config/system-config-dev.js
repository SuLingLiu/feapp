/**
 * dev环境系统配置
 */
module.exports = {
    //构建项目根路径
    buildProjectRootDir: "E:/myBuild/gfe/feapp",
    //config.json文件暂时存放地址
    configTempDir:"E:\\config",
    //映射目录(服务器上传到FTP包时，不需要连接服务器，做了服务器目录映射，可以直接拷贝文件到ftp映射的目录)
    mapUrl:"E:/package-bak"
}
